# Load-Balancer
CSCE 412: Load Balancer Simulation

To run: 

inside root directory run ```make```

cd into build 

run ```./Load-Balancer```

Note:

Make sure top run ```make clean``` to remove all previous log and stats. These files will build up between multiple simulations